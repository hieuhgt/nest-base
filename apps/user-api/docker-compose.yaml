version: '3.8'

services:
  nestjs-api:
    container_name: user-api
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - 3002:3002
    env_file:
      - ./.env
